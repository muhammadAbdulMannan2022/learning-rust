fn main() {
    // let x = 2.0; // f64

    // let y: f32 = 3.0; // f32
    // let f: bool = false;
    //  let z: char = 'ℤ';
    let tup: (i32, f64, u8) = (500, 6.4, 1);
    let (x, y, z) = tup;
    let five_hundred = x.0;

    // let a = [1, 2, 3, 4, 5];
    let a: [i32; 5] = [1, 2, 3, 4, 5];
}
